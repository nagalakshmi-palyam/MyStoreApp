<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lakshmi.myapplication">
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.TypesofProductsActivity"></activity>
        <activity android:name=".Activities.CollectionName" />
        <activity android:name=".Activities.AddCollectionActivity" />
        <activity android:name=".Activities.VideoActivity" />
        <activity android:name=".Activities.HelpLineActivity" />
        <activity android:name=".Activities.StoreProfile" />
        <activity android:name=".Activities.BusinessActivity" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.ChooseLagnguageActivity" />
        <activity android:name=".Activities.PhoneNumberAuthentication" />
        <activity android:name=".Activities.AddProduct" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>